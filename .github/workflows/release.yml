name: Release

on:
  pull_request:
    branches:
      - main
    types:
      - closed

permissions:
  contents: write
  pull-requests: read

jobs:
  release:
    name: Build and Publish Release Artifact
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.bump.outputs.version }}
      tag: ${{ steps.bump.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up JDK 26
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '26'
          cache: maven

      - name: Resolve semver bump type from PR labels
        id: bump-type
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          PR_LABELS_JSON: ${{ toJson(github.event.pull_request.labels) }}
          MERGE_SHA: ${{ github.event.pull_request.merge_commit_sha }}
        run: |
          set -euo pipefail
          TITLE="$PR_TITLE"
          BODY="$PR_BODY"
          LABELS="$(echo "$PR_LABELS_JSON" | jq -r '.[].name' || true)"
          BUMP="patch"
          if echo "$LABELS" | grep -qw 'version:major'; then
            BUMP="major"
          elif echo "$LABELS" | grep -qw 'version:minor'; then
            BUMP="minor"
          elif echo "$LABELS" | grep -qw 'version:patch'; then
            BUMP="patch"
          fi
          if [[ -z "$TITLE" ]]; then
            TITLE="Release ${MERGE_SHA}"
          fi
          if [[ -z "$BODY" ]]; then
            BODY="Release created from merged PR"
          fi
          echo "type=$BUMP" >> "$GITHUB_OUTPUT"
          echo "title=$TITLE" >> "$GITHUB_OUTPUT"
          {
            echo "body<<EOF"
            echo "$BODY"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Bump pom.xml version and create release tag
        id: bump
        env:
          BUMP_TYPE: ${{ steps.bump-type.outputs.type }}
        run: |
          set -euo pipefail
          CURRENT="$(mvn -q -DforceStdout help:evaluate -Dexpression=project.version)"
          BASE="${CURRENT%-SNAPSHOT}"
          IFS='.' read -r MAJOR MINOR PATCH <<< "$BASE"
          if [[ -z "$MAJOR" || -z "$MINOR" || -z "$PATCH" ]]; then
            echo "Unsupported semver format: $BASE"
            exit 1
          fi
          case "$BUMP_TYPE" in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            patch|*)
              PATCH=$((PATCH + 1))
              ;;
          esac
          NEXT="${MAJOR}.${MINOR}.${PATCH}"
          mvn -B -ntp versions:set -DnewVersion="$NEXT" -DgenerateBackupPoms=false
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pom.xml
          if git diff --cached --quiet; then
            echo "No version bump changes detected"
            echo "version=$NEXT" >> "$GITHUB_OUTPUT"
            echo "tag=v$NEXT" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "Bump version to $NEXT"
          git tag "v$NEXT"
          git push origin HEAD:main --follow-tags
          echo "version=$NEXT" >> "$GITHUB_OUTPUT"
          echo "tag=v$NEXT" >> "$GITHUB_OUTPUT"

      - name: Package application
        run: mvn -B -ntp -DskipTests package

      - name: Create checksums
        run: sha256sum target/*.jar > target/sha256sums.txt

      - name: Generate changelog file
        run: |
          echo "# Changelog" > changelog.md
          echo "" >> changelog.md
          echo "## ${{ steps.bump-type.outputs.title }}" >> changelog.md
          echo "" >> changelog.md
          echo "${{ steps.bump-type.outputs.body }}" >> changelog.md

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.bump.outputs.tag }}
          name: ${{ steps.bump.outputs.tag }}
          body_path: changelog.md
          files: |
            target/*.jar
            target/sha256sums.txt
            changelog.md
